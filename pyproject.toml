[project]
name = "gepa-ai-example"
version = "0.1.0"
description = "Multi-agent prompt optimization lab using GEPA"
requires-python = ">=3.10"
dependencies = [
    "gepa[full]",
    "langgraph-sdk",
]

[project.scripts]
gepa-optimize = "main:main"
